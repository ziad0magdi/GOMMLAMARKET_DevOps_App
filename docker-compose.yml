version: "3.8"

services:
  backend:
    build: ./server
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
      - DB_HOST=10.174.6.6
      - DB_USER=mohammed
      - DB_PASSWORD=123456
      - DB_NAME=retail_t
    depends_on:
      - sqlserver

  frontend:
    build: ./Client
    ports:
      - "3000:80"

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - "1433:1433"
    environment:
      SA_PASSWORD: "##Ziad##Magdi##3"
      ACCEPT_EULA: "Y"
